class Box {
    private:
       int length;
       int width;
       int height;
    public:
       // write prototypes of setters for length, width and height
    void setMesurement1(int plength);
    void setMesurement2(int pwidth);
    void setMesurement3(int pheight);
       // write prototypes of getters for length, width and height
      int getlength();
      int getwidth();
      int getheight();
      int calcVolume();
};
